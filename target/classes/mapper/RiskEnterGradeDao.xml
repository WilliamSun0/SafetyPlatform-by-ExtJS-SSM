<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.safetyplatform.dao.RiskEnterGradeDao">
    <!--目的:为dao接口方法提供sql语句配置
    即针对dao接口中的方法编写我们的sql语句-->


    <!--<update id="reduceNumber">-->
        <!--UPDATE seckill-->
        <!--SET numbers = numbers-1-->
        <!--WHERE seckill_id=#{seckillId}-->
        <!--AND start_time <![CDATA[ <= ]]> #{killTime}-->
        <!--AND end_time >= #{killTime}-->
        <!--AND numbers > 0;-->
    <!--</update>-->

    <!--<select id="queryById" resultType="Seckill" parameterType="long">-->
        <!--SELECT *-->
        <!--FROM seckill-->
        <!--WHERE seckill_id=#{seckillId}-->
    <!--</select>-->

    <select id="queryTest" resultType="Test">
        SELECT *
        FROM test_info
        where test_info.name=#{regionId}
    </select>

    <select id="queryAll" resultType="RiskEnterGrade">
select name as enter_name, fatal_danger_source,high_risk_enterprise,
   danger_chemical_tech,
   restricted_space,
   dust_explosion,
   type_name as enter_industry,
   adi.text as enter_community,
   count(ehr.risk_id) as risk_num

   from enterprise_base_info ebi,enter_business_relation ebr,business_type_info bti,admin_division_info adi,enter_hidden_relation ehr
   where ebi.enter_id = ebr.enter_id and ebr.type_id = bti.type_id and adi.id = ebi.gov_id and ebi.enter_id = ehr.enter_id;
    </select>



</mapper>