<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.safetyplatform.dao.ExecuteLawGovFileDao">
    <!--目的:为dao接口方法提供sql语句配置
    即针对dao接口中的方法编写我们的sql语句-->


    <select id="getExeFiles" resultType="ExecuteLawFile">

        select* from execute_law_file where exe_id = #{exeId};
    </select>

    <select id="getExeGovList" resultType="ExeGovRelation">

       select rid,exe_id,gd.gov_depart_id,gd.gov_depart_name from exe_gov_relation egr right join gov_depart gd on egr.gov_depart_id = gd.gov_depart_id and egr.exe_id = #{exeId};

    </select>

    <delete id="deleteExeGov" >
        DELETE FROM exe_gov_relation WHERE rid = #{rid}
    </delete>


    <insert id="insertExeGov">


        insert into exe_gov_relation
        (exe_id, gov_depart_id)
        values(
            #{exeId},
            #{govId})
    </insert>

</mapper>